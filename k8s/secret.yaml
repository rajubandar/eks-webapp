# Secret to store sensitive application configuration
apiVersion: v1
kind: Secret
metadata:
  # Secret name
  name: webapp-secret
  # Namespace where the Secret lives
  namespace: webapp
type: Opaque
data:
  # Placeholder API key (base64-encoded string).
  # Value here is base64("placeholder_api_key").
  # Replace with your own base64-encoded secret when needed.
  API_KEY: "cGxhY2Vob2xkZXJfYXBpX2tleQ=="
